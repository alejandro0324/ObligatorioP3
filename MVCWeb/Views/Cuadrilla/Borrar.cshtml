@model CommonSolution.DTOs.DTO_Cuadrilla

@{
    ViewBag.Title = "Borrar";
}

<h2>Borrar</h2>

<h3>¿Seguro que quiere cambiar estado de la cuadrilla?</h3>
<div>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.numero)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.numero)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.nombre)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.nombre)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.cantidadPeones)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.cantidadPeones)
        </dd>

    </dl>

    @using (Html.BeginForm("CambiarEstado", "Cuadrilla", new { numero = Model.numero }))
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Cambiar" class="btn btn-default" /> |
            @Html.ActionLink("Volver a la lista", "Listar")
        </div>
    }
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
